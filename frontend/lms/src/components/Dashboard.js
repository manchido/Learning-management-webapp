import React from 'react';

const Dashboard = () => {
  return (
    <div className="dashboard">
      <h2>Dashboard</h2>
      {/* Add JSX for displaying upcoming deadlines, recent activity, etc. */}
    </div>
  );
}

export default Dashboard;
